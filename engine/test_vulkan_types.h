#ifndef TEST_VULKAN_TYPES_H
#define TEST_VULKAN_TYPES_H

#include <vulkan/vulkan.h>

// Test function to see if Vulkan types are recognized
void test_vulkan_types(VkDevice device, VkImageLayout layout, VkDeviceSize size);

#endif // TEST_VULKAN_TYPES_H